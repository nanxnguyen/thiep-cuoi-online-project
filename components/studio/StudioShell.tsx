"use client";

import { useState } from "react";

const steps = ["Chọn phong cách", "Thông tin chính", "Câu chuyện", "Khách mời", "Nhạc nền", "Hoàn tất"];
export function StudioShell() { const [step, setStep] = useState(0); return <main className="studio-shell"><aside className="studio-sidebar"><p className="eyebrow">WEDDING STUDIO</p><h1>Tạo thiệp</h1><div className="studio-progress">Tiến độ hoàn thiện <strong>{Math.round(((step + 1) / steps.length) * 100)}%</strong></div><nav>{steps.map((label, index) => <button className={index === step ? "active" : ""} key={label} onClick={() => setStep(index)}><span>0{index + 1}</span>{label}</button>)}</nav></aside><section className="studio-content"><p className="eyebrow">BƯỚC 0{step + 1} / 06</p><h2>{steps[step]}</h2><p className="lede">Tách từng bước thành component độc lập để dễ mở rộng và tối ưu hydration.</p><div className="studio-card"><h3>{step === 0 ? "Bộ sưu tập của MỘC" : steps[step]}</h3><p>Đây là vùng nội dung của step. Các control tương tác sẽ được tách thành component nhỏ ở bước tiếp theo.</p></div><div className="studio-actions"><button disabled={step === 0} onClick={() => setStep((value) => value - 1)}>← Quay lại</button><button className="button-primary" onClick={() => setStep((value) => Math.min(value + 1, steps.length - 1))}>{step === steps.length - 1 ? "Xem thiệp" : "Tiếp tục →"}</button></div></section></main>; }
